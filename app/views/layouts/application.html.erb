<!DOCTYPE html>
<html>
  <head>
    <title>Timemanagerpro</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-secondary">
      <div class="container-fluid">
        <a class="navbar-brand text-white" href="#"><i class="bi bi-stopwatch-fill"></i> TimeManagerPro</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link active text-white" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
                <%= link_to 'Ver Serviços', services_path, class: "nav-link text-white" %>
            </li>
            <li class="nav-item">
                <%= link_to 'Clientes', clients_path, class: "nav-link text-white" %>
            </li>
            <li class="nav-item">
                <%= link_to 'Agendamentos', appointments_path, class: "nav-link text-white" %>
            </li>
        <li class="nav-item">
      <%= link_to appointments_path, class: "nav-link text-white" do %>
        <i class="bi bi-bell"></i>
        <% if Appointment.any? { |appointment| appointment.near_schedule? } %>
          <span class="badge bg-danger">!</span>
        <% end %>
      <% end %>
    </li>
          </ul>
          <!-- Ícone de notificação -->
        </div>
      </div>
    </nav>

    <%= yield %>

    <!-- Modal de notificação de agendamentos -->
    <div class="modal fade" id="appointmentNotificationModal" tabindex="-1" aria-labelledby="appointmentNotificationModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="appointmentNotificationModalLabel">Upcoming Appointments</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            <% @upcoming_appointments.each do |appointment| %>
              <p><strong><%= appointment.date.strftime("%Y-%m-%d %H:%M") %></strong>: <%= appointment.description %></p>
            <% end %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
          </div>
        </div>
      </div>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  </body>
</html>
