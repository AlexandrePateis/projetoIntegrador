    <div class="modal fade" id="upcomingAppointmentsModal" tabindex="-1" aria-labelledby="upcomingAppointmentsModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="upcomingAppointmentsModalLabel">Próximos agendamentos</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body" id="upcomingAppointmentsModalContent">
            <% @upcoming_appointments.each do |appointment| %>
              <p><%= appointment.client.name %> - <%= appointment.service.name %> às <%= appointment.datetime.strftime("%d/%m/%Y %H:%M") %></p>
            <% end %>
          </div>
        </div>
      </div>
    </div>