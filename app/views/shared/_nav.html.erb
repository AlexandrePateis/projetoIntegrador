<nav class="navbar navbar-expand-lg bg-secondary">
  <div class="container-fluid">
    <a class="navbar-brand text-white bebas-neue-regular" href="#"><i class="bi bi-stopwatch-fill"></i> TimeManagerPro</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active text-white" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <%= link_to 'Ver Serviços', services_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Clientes', clients_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Agendamentos', appointments_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Relatório', reports_path, class: "nav-link text-white" %>
          </li>
          <% if user_signed_in? %>
          <li class="nav-item">
            <button id="notification-bell" class="btn btn-link nav-link text-white" data-bs-toggle="modal" data-bs-target="#upcomingAppointmentsModal">
              <i class="bi bi-bell"></i>
                <% if @upcoming_appointments.any? %>
                  <span class="badge bg-danger rounded-circle"><%= @upcoming_appointments.count %></span>
                <% end %>
            </button>
          </li>
          <%end%>
          <% if user_signed_in? %>
          <div class="dropdown float-end">
            <button class="btn btn-secondary dropdown-toggle me-5" type="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.name %>
            </button>
            <ul class="dropdown-menu">
              <li>
                  <%= link_to "Editar", edit_user_registration_path, class:"dropdown-item " %> 
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= link_to "Sair", destroy_user_session_path, method: :delete, class:"dropdown-item" %>
              </li>
            </ul>
          </div>
          <% else %>
              <%= link_to "Entrar", new_user_session_path, class:"btn text-white" %>
              <%= link_to "Criar conta", new_user_registration_path, method: :delete, class:"btn text-white" %>
          <%end%>
        </ul>
      </div>
  </div>
</nav>
<style>
* {
  font-family: "Jura", sans-serif;
  font-optical-sizing: auto;
  font-weight: <weight>;
  font-style: normal;
}
</style>